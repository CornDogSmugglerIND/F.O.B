{
  "name": "fob",
  "version": "0.1.0",
  "description": "F.O.B — a minimal Express web app used as the starter codebase and Cloud Agent environment demo.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
